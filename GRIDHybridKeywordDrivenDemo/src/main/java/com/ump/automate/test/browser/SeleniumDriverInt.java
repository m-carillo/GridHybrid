package com.ump.automate.test.browser;

import org.openqa.selenium.WebDriver;

public interface SeleniumDriverInt {

    public WebDriver getDriver();

    public void setDriver(WebDriver driver);
}
